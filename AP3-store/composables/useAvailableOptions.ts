import type { Variant } from '~/types'

export function useAvailableOptions(availableVariants: Variant[], variants: Variant[], optionId: number) {
    let availableOptions = [];

    return availableOptions
}